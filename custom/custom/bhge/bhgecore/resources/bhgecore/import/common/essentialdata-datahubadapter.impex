# Remove first section once platform moved back to singular scop and registeredRedirectUri
INSERT_UPDATE OAuthClientDetails;clientId[unique=true]	;resourceIds	;scope		;authorizedGrantTypes											;authorities			;clientSecret	;registeredRedirectUri
                                ;eic					;hybris			;basic		;implicit,client_credentials,password							;ROLE_CLIENT			;secret			;http://localhost:9001/oauth2_implicit_callback;

INSERT_UPDATE OAuthClientDetails;clientId[unique=true]	;resourceIds	;scope		;authorizedGrantTypes											;authorities			;clientSecret	;registeredRedirectUri
;eic					;hybris			;basic		;implicit,client_credentials,password							;ROLE_CLIENT			;secret			;http://localhost:9001/oauth2_implicit_callback;

# Remove first section once platform moved back to singular scop and registeredRedirectUri
INSERT_UPDATE OAuthClientDetails;clientId[unique=true]	;resourceIds	;scope		;authorizedGrantTypes											;authorities			;clientSecret	;registeredRedirectUri
                                ;datahub					;hybris			;basic		;implicit,client_credentials,password							;ROLE_CLIENT			;datahub			;http://localhost:9001/oauth2_implicit_callback;

INSERT_UPDATE OAuthClientDetails;clientId[unique=true]	;resourceIds	;scope		;authorizedGrantTypes											;authorities			;clientSecret	;registeredRedirectUri
;datahub					;hybris			;basic		;implicit,client_credentials,password							;ROLE_CLIENT			;datahub			;http://localhost:9001/oauth2_implicit_callback;
