import { RmaProductPipe } from './rma-product.pipe';

describe('RmaProductPipe', () => {
  it('create an instance', () => {
    const pipe = new RmaProductPipe();
    expect(pipe).toBeTruthy();
  });
});
