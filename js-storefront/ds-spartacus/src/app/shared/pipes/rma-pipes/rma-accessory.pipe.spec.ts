import { RmaAccessoryPipe } from './rma-accessory.pipe';

describe('RmaAccessoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RmaAccessoryPipe();
    expect(pipe).toBeTruthy();
  });
});
